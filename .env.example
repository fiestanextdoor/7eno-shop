PRINTFUL_API_KEY=your_printful_api_key
# Set to "true" to auto-submit paid Printful orders for fulfillment (skips the
# manual draft/confirm step). Requires a payment method or Wallet balance in
# Printful billing. Leave unset/false while testing so orders stay deletable drafts.
PRINTFUL_AUTO_CONFIRM=false
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Coming-soon lock: until the launch moment in lib/release.ts the whole shop is
# sealed behind /coming-soon and no orders can be placed. Set a secret to let
# yourself preview the real site before launch by visiting any URL with
# ?preview=THIS_VALUE (stores a bypass cookie). Leave unset to disable previewing.
COMING_SOON_PREVIEW_SECRET=
